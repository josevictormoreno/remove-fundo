# Fundo Transparente com Python

Remove fundo das imagens utilizando python e a biblioteca pillow

## Setup

É necessário instalar a biblioteca pillow, segue comando:
```bash
$ pip install pillow
```
